@model IEnumerable<WatchMaker.Models.Categoria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create", "", new { @class = "btn btn-success" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idCate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Categoria1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.idCate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Categoria1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idCate}, new { @class = "btn btn-warning" }) |
            @Html.ActionLink("Details", "Details", new { id=item.idCate }, new { @class = "btn btn-primary" }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idCate  }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
