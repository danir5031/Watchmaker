@model IEnumerable<WatchMaker.Models.Tallas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create", "", new { @class = "btn btn-success" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idtalla)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Talla)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.idtalla)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Talla)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idtalla}, new { @class = "btn btn-warning" }) |
            @Html.ActionLink("Details", "Details", new { id=item.idtalla }, new { @class = "btn btn-primary" }) |
            @Html.ActionLink("Delete", "Delete", new {  id=item.idtalla }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
